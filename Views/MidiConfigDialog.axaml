<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:NetKeyer.ViewModels"
        xmlns:res="clr-namespace:NetKeyer.Resources.Strings"
        x:Class="NetKeyer.Views.MidiConfigDialog"
        x:DataType="vm:MidiConfigDialogViewModel"
        Title="{x:Static res:Resources.MidiConfig_Title}"
        Width="650" Height="550"
        MinWidth="650" MinHeight="450"
        WindowStartupLocation="CenterOwner"
        CanResize="true">

    <Grid Margin="15" RowDefinitions="Auto,Auto,*,Auto,Auto">

        <!-- Title and Instructions -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,15">
            <TextBlock Text="{x:Static res:Resources.MidiConfig_Title}"
                       FontSize="18"
                       FontWeight="Bold"/>
            <TextBlock Text="{x:Static res:Resources.MidiConfig_Instructions}"
                       FontSize="12"
                       Foreground="Gray"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Add Note Section -->
        <Border Grid.Row="1"
                BorderBrush="Gray"
                BorderThickness="1"
                Padding="10"
                CornerRadius="3"
                Margin="0,0,0,10">
            <StackPanel Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock Text="{x:Static res:Resources.MidiConfig_AddNote}"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"/>
                    <NumericUpDown Value="{Binding SelectedNoteNumber}"
                                   Minimum="0"
                                   Maximum="127"
                                   Width="120"
                                   ShowButtonSpinner="True"
                                   FormatString="0"
                                   AllowSpin="True"
                                   ParsingNumberStyle="Integer"/>
                    <Button Content="{x:Static res:Resources.MidiConfig_Add}"
                            Command="{Binding AddNoteCommand}"
                            Width="60"
                            HorizontalContentAlignment="Center"
                            Padding="5"/>
                    <TextBlock Text="{x:Static res:Resources.MidiConfig_NoteRange}"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Foreground="Gray"/>
                </StackPanel>
                <TextBlock Text="{Binding AddNoteError}"
                           Foreground="Red"
                           FontSize="11"
                           IsVisible="{Binding AddNoteError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                           Margin="0,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Note Configurations Table -->
        <Border Grid.Row="2"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="3">
            <ScrollViewer>
                <StackPanel>
                    <!-- Header Row -->
                    <Border BorderBrush="LightGray"
                            BorderThickness="0,0,0,1"
                            Background="#F5F5F5"
                            Padding="8,6">
                        <Grid ColumnDefinitions="70,90,90,90,90,60">
                            <TextBlock Grid.Column="0" HorizontalAlignment="Center" Text="{x:Static res:Resources.MidiConfig_NoteNumber}" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Grid.Column="1" HorizontalAlignment="Center" Text="{x:Static res:Resources.MidiConfig_LeftPaddle}" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Grid.Column="2" HorizontalAlignment="Center" Text="{x:Static res:Resources.MidiConfig_RightPaddle}" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Grid.Column="3" HorizontalAlignment="Center" Text="{x:Static res:Resources.MidiConfig_StraightKey}" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Grid.Column="4" HorizontalAlignment="Center" Text="{x:Static res:Resources.MidiConfig_PTT}" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Grid.Column="5" HorizontalAlignment="Center" Text="{x:Static res:Resources.MidiConfig_Remove}" FontWeight="Bold" FontSize="12"/>
                        </Grid>
                    </Border>

                    <!-- Data Rows -->
                    <ItemsControl ItemsSource="{Binding NoteConfigurations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:MidiNoteConfigRow">
                                <Border BorderBrush="LightGray"
                                        BorderThickness="0,0,0,1"
                                        Padding="8,6">
                                    <Grid ColumnDefinitions="70,90,90,90,90,60">
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding NoteNumber}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontFamily="Consolas,Courier New,monospace"/>
                                        <CheckBox Grid.Column="1"
                                                  IsChecked="{Binding IsLeftPaddle}"
                                                  HorizontalAlignment="Center"/>
                                        <CheckBox Grid.Column="2"
                                                  IsChecked="{Binding IsRightPaddle}"
                                                  HorizontalAlignment="Center"/>
                                        <CheckBox Grid.Column="3"
                                                  IsChecked="{Binding IsStraightKey}"
                                                  HorizontalAlignment="Center"/>
                                        <CheckBox Grid.Column="4"
                                                  IsChecked="{Binding IsPtt}"
                                                  HorizontalAlignment="Center"/>
                                        <Button Grid.Column="5"
                                                Content="×"
                                                Command="{Binding $parent[Window].DataContext.RemoveNoteCommand}"
                                                CommandParameter="{Binding}"
                                                FontSize="16"
                                                Width="30"
                                                Height="25"
                                                Padding="0"
                                                HorizontalAlignment="Center"
                                                HorizontalContentAlignment="Center"
                                                VerticalContentAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Function Descriptions -->
        <Border Grid.Row="3"
                BorderBrush="LightGray"
                BorderThickness="1"
                Background="#FAFAFA"
                Padding="10"
                CornerRadius="3"
                Margin="0,10,0,0">
            <StackPanel Spacing="4">
                <TextBlock Text="{x:Static res:Resources.MidiConfig_FunctionDescriptions}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,3"/>
                <TextBlock FontSize="10" Foreground="#444">• <Run Text="{x:Static res:Resources.MidiConfig_LeftPaddleDesc}"/></TextBlock>
                <TextBlock FontSize="10" Foreground="#444">• <Run Text="{x:Static res:Resources.MidiConfig_RightPaddleDesc}"/></TextBlock>
                <TextBlock FontSize="10" Foreground="#444">• <Run Text="{x:Static res:Resources.MidiConfig_StraightKeyDesc}"/></TextBlock>
                <TextBlock FontSize="10" Foreground="#444">• <Run Text="{x:Static res:Resources.MidiConfig_PTTDesc}"/></TextBlock>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,15,0,0">
            <Button Content="{x:Static res:Resources.MidiConfig_ResetToDefaults}"
                    Command="{Binding ResetToDefaultsCommand}"
                    Width="130"
                    HorizontalContentAlignment="Center"
                    Padding="6"/>
            <Button Name="CancelButton"
                    Content="{x:Static res:Resources.MidiConfig_Cancel}"
                    Width="80"
                    HorizontalContentAlignment="Center"
                    Padding="6"
                    Click="CancelButton_Click"/>
            <Button Name="OkButton"
                    Content="{x:Static res:Resources.MidiConfig_OK}"
                    Width="80"
                    HorizontalContentAlignment="Center"
                    Padding="6"
                    Click="OkButton_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
